package br.com.virtualab.app.controller.type;

import java.util.LinkedList;
import java.util.List;

import javax.faces.model.SelectItem;

import br.com.virtualab.app.controller.Abstract.AbstractController;

public class TypeController extends AbstractController {

    private static final long serialVersionUID = 1L;
/*
    public List<SelectItem> getItemsTemaType() {
        List<SelectItem> items = new LinkedList<SelectItem>();
        for (TemaType enumType : TemaType.values()) {
            items.add(new SelectItem(enumType.name(), enumType.getDescricao()));
        }
        return items;
    }
    */
}
